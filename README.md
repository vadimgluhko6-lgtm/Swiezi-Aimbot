# Swiezi-Aimbot

local Players = game:GetService("Players") local RunService = game:GetService("RunService") local UserInputService = game:GetService("UserInputService") local CoreGui = game:GetService("CoreGui") local Lighting = game:GetService("Lighting") local TweenService = game:GetService("TweenService") local Camera = workspace.CurrentCamera local LocalPlayer = Players.LocalPlayer local Config = { Aimbot = { Enabled = false, Smoothness = 5, FOV = 100, TargetPart = "Head", ShowFOV = false, FOVColor = Color3.fromRGB(0, 220, 255), WallCheck = true, TeamCheck = true }, ESP = { Enabled = false, Highlight = false, Names = false, Items = false, Health = false, Rainbow = false, Skeleton = false, TeamCheck = true, WallCheck = false, Color = Color3.fromRGB(0, 255, 150) }, Cosmetics = { Rainbow3DCape = false, RainbowLantern = false, Fireflies = true }, Menu = { Rainbow = true, ThemeColor = Color3.fromRGB(12, 11, 16) }, Settings = { FollowMouse = false } } local MenuState = false local ActiveTab = "AIM" local LastRandomBone = "Head" local ActiveRunning = true local VisibilityCache = {} local ScreenGui = Instance.new("ScreenGui") ScreenGui.Name = "TWKS_Engine_V2" ScreenGui.DisplayOrder = 999999 ScreenGui.IgnoreGuiInset = true ScreenGui.Parent = CoreGui local HUDContainer = Instance.new("Frame", ScreenGui) HUDContainer.Size = UDim2.new(1, 0, 1, 0) HUDContainer.BackgroundTransparency = 1 local FOV_UI = Instance.new("Frame", HUDContainer) FOV_UI.AnchorPoint = Vector2.new(0.5, 0.5) FOV_UI.BackgroundTransparency = 1 FOV_UI.Visible = false FOV_UI.ZIndex = 100 local FOV_Stroke = Instance.new("UIStroke", FOV_UI) FOV_Stroke.Thickness = 1.5 FOV_Stroke.Color = Config.Aimbot.FOVColor Instance.new("UICorner", FOV_UI).CornerRadius = UDim.new(1, 0) local Cursor_UI = Instance.new("Frame", HUDContainer) Cursor_UI.Size = UDim2.new(0, 40, 0, 40) Cursor_UI.AnchorPoint = Vector2.new(0.5, 0.5) Cursor_UI.BackgroundTransparency = 1 Cursor_UI.ZIndex = 50000 local CenterDot = Instance.new("Frame", Cursor_UI) CenterDot.Size = UDim2.new(0, 4, 0, 4) CenterDot.Position = UDim2.new(0.5, -2, 0.5, -2) CenterDot.BackgroundColor3 = Color3.fromRGB(0, 220, 255) CenterDot.BorderSizePixel = 0 CenterDot.ZIndex = 50001 Instance.new("UICorner", CenterDot).CornerRadius = UDim.new(1, 0) for i = 1, 4 do local L = Instance.new("Frame", Cursor_UI) L.BorderSizePixel = 0 L.AnchorPoint = Vector2.new(0.5, 0.5) L.BackgroundColor3 = Color3.fromRGB(0, 220, 255) L.ZIndex = 50001 if i == 1 then L.Size = UDim2.new(0, 2, 0, 12) L.Position = UDim2.new(0.5, 0, 0.2, 0) elseif i == 2 then L.Size = UDim2.new(0, 2, 0, 12) L.Position = UDim2.new(0.5, 0, 0.8, 0) elseif i == 3 then L.Size = UDim2.new(0, 12, 0, 2) L.Position = UDim2.new(0.2, 0, 0.5, 0) else L.Size = UDim2.new(0, 12, 0, 2) L.Position = UDim2.new(0.8, 0, 0.5, 0) end end local RainbowHue = 0 local CursorRot = 0 local function IsVisibleRay(TargetPart, Character) if not TargetPart then return true end local Origin = Camera.CFrame.Position local Direction = TargetPart.Position - Origin local Params = RaycastParams.new() Params.FilterType = Enum.RaycastFilterType.Exclude Params.FilterDescendantsInstances = {LocalPlayer.Character, Character, ScreenGui} local Result = workspace:Raycast(Origin, Direction, Params) if Result then return Result.Instance:IsDescendantOf(Character) end return true end local function CheckTeam(Player) if Player.Team == LocalPlayer.Team and Player.Team ~= nil then return false end return true end local function GetClosestPlayer() local Closest = nil local ShortestDistance = Config.Aimbot.FOV local MousePos = UserInputService:GetMouseLocation() local CenterScreen = Camera.ViewportSize / 2 local ReferencePos = (MenuState or Config.Settings.FollowMouse) and MousePos or CenterScreen for _, Player in pairs(Players:GetPlayers()) do if Player ~= LocalPlayer and Player.Character then if Config.Aimbot.TeamCheck and not CheckTeam(Player) then continue end local Hum = Player.Character:FindFirstChildOfClass("Humanoid") if Hum and Hum.Health > 0 then local TargetPartName = Config.Aimbot.TargetPart if TargetPartName == "Random" then TargetPartName = LastRandomBone end local TargetPart = Player.Character:FindFirstChild(TargetPartName) if TargetPart then local isTargetVisible = true if Config.Aimbot.WallCheck then isTargetVisible = VisibilityCache[Player] or false end if isTargetVisible then local Pos, OnScreen = Camera:WorldToViewportPoint(TargetPart.Position) if OnScreen then local Distance = (Vector2.new(Pos.X, Pos.Y) - ReferencePos).Magnitude if Distance < ShortestDistance then Closest = TargetPart ShortestDistance = Distance end end end end end end end return Closest end local BackgroundBlur = Instance.new("Frame", ScreenGui) BackgroundBlur.Size = UDim2.new(1, 0, 1, 0) BackgroundBlur.BackgroundColor3 = Color3.fromRGB(3, 3, 5) BackgroundBlur.BackgroundTransparency = 1 BackgroundBlur.Visible = false local InGameBlur = Lighting:FindFirstChild("BloxSis_Blur") or Instance.new("BlurEffect") InGameBlur.Name = "BloxSis_Blur" InGameBlur.Size = 0 InGameBlur.Parent = Lighting local MainFrame = Instance.new("Frame", ScreenGui) MainFrame.Size = UDim2.new(0, 0, 0, 0) MainFrame.Position = UDim2.new(0.5, 0, 0.5, 0) MainFrame.BackgroundColor3 = Config.Menu.ThemeColor MainFrame.BorderSizePixel = 0 MainFrame.ClipsDescendants = true MainFrame.ZIndex = 1 local MainGradient = Instance.new("UIGradient", MainFrame) MainGradient.Rotation = 45 local MainStroke = Instance.new("UIStroke", MainFrame) MainStroke.Thickness = 1.5 MainStroke.ZIndex = 2 Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10) local TopBar = Instance.new("Frame", MainFrame) TopBar.Size = UDim2.new(1, 0, 0, 50) TopBar.BackgroundColor3 = Color3.fromRGB(18, 16, 24) TopBar.BorderSizePixel = 0 TopBar.ZIndex = 2 local Title = Instance.new("TextLabel", TopBar) Title.Size = UDim2.new(0, 140, 1, 0) Title.Position = UDim2.new(0, 16, 0, 0) Title.Text = "BloxSis" Title.TextColor3 = Color3.fromRGB(240, 240, 245) Title.Font = Enum.Font.GothamBold Title.TextSize = 15 Title.TextXAlignment = Enum.TextXAlignment.Left Title.BackgroundTransparency = 1 Title.ZIndex = 3 local DragButton = Instance.new("TextButton", TopBar) DragButton.Size = UDim2.new(1, -60, 1, 0) DragButton.BackgroundTransparency = 1 DragButton.Text = "" DragButton.ZIndex = 4 local CloseBtn = Instance.new("TextButton", TopBar) CloseBtn.Size = UDim2.new(0, 10, 0, 10) CloseBtn.Position = UDim2.new(1, -24, 0.5, -5) CloseBtn.BackgroundColor3 = Color3.fromRGB(255, 75, 75) CloseBtn.Text = "" CloseBtn.ZIndex = 10 Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(1, 0) local Dragging, DragInput, DragStart, StartPos DragButton.InputBegan:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then Dragging = true DragStart = input.Position StartPos = MainFrame.Position input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then Dragging = false end end) end end) DragButton.InputChanged:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseMovement then DragInput = input end end) UserInputService.InputChanged:Connect(function(input) if input == DragInput and Dragging then local Delta = input.Position - DragStart MainFrame.Position = UDim2.new(StartPos.X.Scale, StartPos.X.Offset + Delta.X, StartPos.Y.Scale, StartPos.Y.Offset + Delta.Y) end end) local CategoriesContainer = Instance.new("Frame", TopBar) CategoriesContainer.Size = UDim2.new(1, -160, 1, 0) CategoriesContainer.Position = UDim2.new(0, 140, 0, 0) CategoriesContainer.BackgroundTransparency = 1 CategoriesContainer.ZIndex = 5 local CatLayout = Instance.new("UIListLayout", CategoriesContainer) CatLayout.FillDirection = Enum.FillDirection.Horizontal CatLayout.VerticalAlignment = Enum.VerticalAlignment.Center CatLayout.Padding = UDim.new(0, 16) local ContentArea = Instance.new("Frame", MainFrame) ContentArea.Size = UDim2.new(1, -32, 1, -110) ContentArea.Position = UDim2.new(0, 16, 0, 85) ContentArea.BackgroundTransparency = 1 ContentArea.ZIndex = 2 local Pages = { AIM = Instance.new("Frame", ContentArea), VISUALS = Instance.new("Frame", ContentArea), COSMETICS = Instance.new("Frame", ContentArea), MISC = Instance.new("Frame", ContentArea) } for name, frame in pairs(Pages) do frame.Size = UDim2.new(1, 0, 1, 0) frame.BackgroundTransparency = 1 frame.Visible = (name == "AIM") frame.ZIndex = 3 end local function CreateGroupbox(Parent, TitleText, XPosition) local Box = Instance.new("Frame", Parent) Box.Size = UDim2.new(0.5, -8, 0, 360) Box.Position = XPosition Box.BackgroundColor3 = Color3.fromRGB(18, 16, 24) Box.ZIndex = 5 Instance.new("UICorner", Box).CornerRadius = UDim.new(0, 6) local Stroke = Instance.new("UIStroke", Box) Stroke.Thickness = 1 Stroke.Color = Color3.fromRGB(40, 35, 50) Stroke.ZIndex = 6 local Lbl = Instance.new("TextLabel", Box) Lbl.Size = UDim2.new(0, 0, 0, 16) Lbl.Position = UDim2.new(0, 12, 0, -8) Lbl.Text = " " .. TitleText .. " " Lbl.TextColor3 = Color3.fromRGB(160, 140, 180) Lbl.Font = Enum.Font.GothamBold Lbl.TextSize = 11 Lbl.TextXAlignment = Enum.TextXAlignment.Left Lbl.BackgroundColor3 = Color3.fromRGB(18, 16, 24) Lbl.AutomaticSize = Enum.AutomaticSize.XY Lbl.ZIndex = 7 local Content = Instance.new("Frame", Box) Content.Size = UDim2.new(1, -20, 1, -20) Content.Position = UDim2.new(0, 10, 0, 14) Content.BackgroundTransparency = 1 Content.ZIndex = 7 return Content end local AimOptionsG = CreateGroupbox(Pages.AIM, "Options", UDim2.new(0, 0, 0, 0)) local DummyG = CreateGroupbox(Pages.AIM, "ИНТЕРАКТИВНЫЙ DUMMY", UDim2.new(0.5, 8, 0, 0)) local VisMainG = CreateGroupbox(Pages.VISUALS, "Main ESP", UDim2.new(0, 0, 0, 0)) local VisPreviewG = CreateGroupbox(Pages.VISUALS, "ESP ИНТЕРАКТИВНОЕ ПРЕВЬЮ", UDim2.new(0.5, 8, 0, 0)) local CosG = CreateGroupbox(Pages.COSMETICS, "Cosmetics", UDim2.new(0, 0, 0, 0)) local MiscThemeG = CreateGroupbox(Pages.MISC, "Menu Customization & Other", UDim2.new(0, 0, 0, 0)) local AimLeftLayout = Instance.new("UIListLayout", AimOptionsG) AimLeftLayout.Padding = UDim.new(0, 10) local VisLeftLayout = Instance.new("UIListLayout", VisMainG) VisLeftLayout.Padding = UDim.new(0, 10) local CosLeftLayout = Instance.new("UIListLayout", CosG) CosLeftLayout.Padding = UDim.new(0, 10) local MiscLayout = Instance.new("UIListLayout", MiscThemeG) MiscLayout.Padding = UDim.new(0, 10) local function CreateToggle(Parent, Text, ConfigTable, Key, Callback) local Frame = Instance.new("Frame", Parent) Frame.Size = UDim2.new(1, 0, 0, 20) Frame.BackgroundTransparency = 1 Frame.ZIndex = 7 local Btn = Instance.new("TextButton", Frame) Btn.Size = UDim2.new(0, 12, 0, 12) Btn.Position = UDim2.new(0, 0, 0.5, -6) Btn.BackgroundColor3 = ConfigTable[Key] and Color3.fromRGB(160, 50, 255) or Color3.fromRGB(30, 25, 40) Btn.Text = "" Btn.ZIndex = 8 Instance.new("UICorner", Btn).CornerRadius = UDim.new(0, 3) local Stroke = Instance.new("UIStroke", Frame) Stroke.Thickness = 1 Stroke.Color = Color3.fromRGB(60, 50, 80) Stroke.ZIndex = 8 local Label = Instance.new("TextLabel", Frame) Label.Size = UDim2.new(1, -20, 1, 0) Label.Position = UDim2.new(0, 20, 0, 0) Label.Text = Text Label.TextColor3 = Color3.fromRGB(200, 195, 210) Label.Font = Enum.Font.GothamMedium Label.TextSize = 12 Label.TextXAlignment = Enum.TextXAlignment.Left Label.BackgroundTransparency = 1 Label.ZIndex = 8 Btn.MouseButton1Click:Connect(function() if not ActiveRunning then return end ConfigTable[Key] = not ConfigTable[Key] Btn.BackgroundColor3 = ConfigTable[Key] and Color3.fromRGB(160, 50, 255) or Color3.fromRGB(30, 25, 40) if Callback then Callback(ConfigTable[Key]) end end) end local function CreateSlider(Parent, Text, Min, Max, Default, ConfigTable, Key) local Frame = Instance.new("Frame", Parent) Frame.Size = UDim2.new(1, 0, 0, 34) Frame.BackgroundTransparency = 1 Frame.ZIndex = 7 local Label = Instance.new("TextLabel", Frame) Label.Size = UDim2.new(1, 0, 0, 16) Label.Text = Text .. ": " .. Default Label.TextColor3 = Color3.fromRGB(180, 175, 190) Label.Font = Enum.Font.GothamMedium Label.TextSize = 11 Label.TextXAlignment = Enum.TextXAlignment.Left Label.BackgroundTransparency = 1 Label.ZIndex = 8 local Bar = Instance.new("TextButton", Frame) Bar.Size = UDim2.new(1, 0, 0, 5) Bar.Position = UDim2.new(0, 0, 0, 22) Bar.BackgroundColor3 = Color3.fromRGB(30, 25, 40) Bar.Text = "" Bar.ZIndex = 8 Instance.new("UICorner", Bar) local Fill = Instance.new("Frame", Bar) Fill.Size = UDim2.new((Default - Min)/(Max - Min), 0, 1, 0) Fill.BackgroundColor3 = Color3.fromRGB(160, 50, 255) Fill.ZIndex = 9 Instance.new("UICorner", Fill) local Move = false local function Update() if not ActiveRunning then return end local MouseX = UserInputService:GetMouseLocation().X local RelativeX = MouseX - Bar.AbsolutePosition.X local Percent = math.clamp(RelativeX / Bar.AbsoluteSize.X, 0, 1) local Value = math.floor(Min + (Max - Min) * Percent) Fill.Size = UDim2.new(Percent, 0, 1, 0) Label.Text = Text .. ": " .. Value ConfigTable[Key] = Value end Bar.InputBegan:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then Move = true Update() end end) UserInputService.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then Move = false end end) UserInputService.InputChanged:Connect(function(input) if Move and input.UserInputType == Enum.UserInputType.MouseMovement then Update() end end) end local P_Container = Instance.new("Frame", VisPreviewG) P_Container.Size = UDim2.new(1, 0, 1, 0) P_Container.BackgroundTransparency = 1 P_Container.ZIndex = 6 local P_Head = Instance.new("Frame", P_Container) P_Head.Size = UDim2.new(0, 36, 0, 36) P_Head.Position = UDim2.new(0, 122, 0, 45) P_Head.BackgroundColor3 = Color3.fromRGB(35, 30, 45) P_Head.ZIndex = 7 Instance.new("UICorner", P_Head).CornerRadius = UDim.new(0, 6) local P_Torso = Instance.new("Frame", P_Container) P_Torso.Size = UDim2.new(0, 72, 0, 72) P_Torso.Position = UDim2.new(0, 104, 0, 87) P_Torso.BackgroundColor3 = Color3.fromRGB(35, 30, 45) P_Torso.ZIndex = 7 Instance.new("UICorner", P_Torso).CornerRadius = UDim.new(0, 6) local P_LArm = Instance.new("Frame", P_Container) P_LArm.Size = UDim2.new(0, 30, 0, 72) P_LArm.Position = UDim2.new(0, 68, 0, 87) P_LArm.BackgroundColor3 = Color3.fromRGB(35, 30, 45) P_LArm.ZIndex = 7 Instance.new("UICorner", P_LArm).CornerRadius = UDim.new(0, 6) local P_RArm = Instance.new("Frame", P_Container) P_RArm.Size = UDim2.new(0, 30, 0, 72) P_RArm.Position = UDim2.new(0, 182, 0, 87) P_RArm.BackgroundColor3 = Color3.fromRGB(35, 30, 45) P_RArm.ZIndex = 7 Instance.new("UICorner", P_RArm).CornerRadius = UDim.new(0, 6) local P_LLeg = Instance.new("Frame", P_Container) P_LLeg.Size = UDim2.new(0, 32, 0, 72) P_LLeg.Position = UDim2.new(0, 104, 0, 165) P_LLeg.BackgroundColor3 = Color3.fromRGB(35, 30, 45) local LLegCorner = Instance.new("UICorner", P_LLeg) LLegCorner.CornerRadius = UDim.new(0, 6) P_LLeg.ZIndex = 7 local P_RLeg = Instance.new("Frame", P_Container) P_RLeg.Size = UDim2.new(0, 32, 0, 72) P_RLeg.Position = UDim2.new(0, 144, 0, 165) P_RLeg.BackgroundColor3 = Color3.fromRGB(35, 30, 45) P_RLeg.ZIndex = 7 Instance.new("UICorner", P_RLeg).CornerRadius = UDim.new(0, 6) local RobloxParts = {P_Head, P_Torso, P_LArm, P_RArm, P_LLeg, P_RLeg} local P_Strokes = {} for _, part in pairs(RobloxParts) do local str = Instance.new("UIStroke", part) str.Thickness = 1.2 str.Color = Config.ESP.Color str.Enabled = false str.ZIndex = 8 table.insert(P_Strokes, str) end local P_Name = Instance.new("TextLabel", P_Container) P_Name.Size = UDim2.new(0, 120, 0, 16) P_Name.Position = UDim2.new(0, 80, 0, 20) P_Name.Text = "Roblox_Player [45m]" P_Name.TextColor3 = Color3.fromRGB(255, 255, 255) P_Name.Font = Enum.Font.GothamBold P_Name.TextSize = 11 P_Name.BackgroundTransparency = 1 P_Name.Visible = false P_Name.ZIndex = 8 local P_HPCont = Instance.new("Frame", P_Container) P_HPCont.Size = UDim2.new(0, 5, 0, 192) P_HPCont.Position = UDim2.new(0, 55, 0, 45) P_HPCont.BackgroundColor3 = Color3.fromRGB(20, 20, 25) P_HPCont.Visible = false P_HPCont.ZIndex = 8 local P_HPFill = Instance.new("Frame", P_HPCont) P_HPFill.Size = UDim2.new(1, 0, 1, 0) P_HPFill.BackgroundColor3 = Color3.fromRGB(0, 255, 100) P_HPFill.BorderSizePixel = 0 P_HPFill.ZIndex = 9 local P_HPText = Instance.new("TextLabel", P_Container) P_HPText.Size = UDim2.new(0, 40, 0, 14) P_HPText.Position = UDim2.new(0, 10, 0, 42) P_HPText.Text = "100 HP" P_HPText.TextColor3 = Color3.fromRGB(0, 255, 100) P_HPText.Font = Enum.Font.GothamBold P_HPText.TextSize = 10 P_HPText.BackgroundTransparency = 1 P_HPText.Visible = false P_HPText.ZIndex = 8 local function UpdatePreview() local active = Config.ESP.Enabled local showChams = (active and Config.ESP.Highlight) for _, part in pairs(RobloxParts) do part.BackgroundTransparency = showChams and 0.5 or 0 end for _, str in pairs(P_Strokes) do str.Enabled = showChams end P_Name.Visible = (active and Config.ESP.Names) P_HPCont.Visible = (active and Config.ESP.Health) P_HPText.Visible = (active and Config.ESP.Health) end CreateToggle(AimOptionsG, "Enable Aimbot Tracking", Config.Aimbot, "Enabled") CreateToggle(AimOptionsG, "Draw FOV Overlay", Config.Aimbot, "ShowFOV") CreateToggle(AimOptionsG, "Wall Check Validation", Config.Aimbot, "WallCheck") CreateToggle(AimOptionsG, "Team Check Validation", Config.Aimbot, "TeamCheck") CreateSlider(AimOptionsG, "Smoothing Factor", 1, 30, 5, Config.Aimbot, "Smoothness") CreateSlider(AimOptionsG, "Lock Radius (FOV)", 10, 600, 100, Config.Aimbot, "FOV") CreateToggle(VisMainG, "Global ESP", Config.ESP, "Enabled", UpdatePreview) CreateToggle(VisMainG, "Chams / Body Highlight", Config.ESP, "Highlight", UpdatePreview) CreateToggle(VisMainG, "Identity Labels", Config.ESP, "Names", UpdatePreview) CreateToggle(VisMainG, "Health Indicators", Config.ESP, "Health", UpdatePreview) CreateToggle(VisMainG, "Skeleton Visuals", Config.ESP, "Skeleton") CreateToggle(VisMainG, "ESP Wall Check", Config.ESP, "WallCheck") CreateToggle(VisMainG, "ESP Team Check", Config.ESP, "TeamCheck") CreateToggle(CosG, "3D Motion Cape (при ходьбе)", Config.Cosmetics, "Rainbow3DCape") CreateToggle(MiscThemeG, "Rainbow Menu Theme", Config.Menu, "Rainbow") CreateToggle(MiscThemeG, "Follow Invisible Mouse", Config.Settings, "FollowMouse") local UnloadBox = Instance.new("TextButton", MiscThemeG) UnloadBox.Size = UDim2.new(1, 0, 0, 30) UnloadBox.BackgroundColor3 = Color3.fromRGB(45, 20, 30) UnloadBox.Text = "Unload Engine" UnloadBox.Font = Enum.Font.GothamBold UnloadBox.TextColor3 = Color3.fromRGB(255, 75, 75) UnloadBox.TextSize = 12 Instance.new("UICorner", UnloadBox).CornerRadius = UDim.new(0, 5) Instance.new("UIStroke", UnloadBox).Color = Color3.fromRGB(80, 35, 45) local DummyIndicator = Instance.new("TextLabel", DummyG) DummyIndicator.Size = UDim2.new(1, 0, 0, 16) DummyIndicator.Position = UDim2.new(0, 0, 0, 245) DummyIndicator.Text = "Active Target: HEAD" DummyIndicator.TextColor3 = Color3.fromRGB(0, 220, 255) DummyIndicator.Font = Enum.Font.GothamBold DummyIndicator.TextSize = 11 DummyIndicator.BackgroundTransparency = 1 DummyIndicator.ZIndex = 6 DummyFrame = Instance.new("Frame", DummyG) DummyFrame.Size = UDim2.new(1, 0, 0, 220) DummyFrame.Position = UDim2.new(0, 0, 0, 15) DummyFrame.BackgroundTransparency = 1 DummyFrame.ZIndex = 6 local function CreateDummyBtn(Txt, Size, Pos, BoneName) local B = Instance.new("TextButton", DummyFrame) B.Size = Size B.Position = Pos B.Text = Txt B.Font = Enum.Font.GothamBold B.TextSize = 11 B.BackgroundColor3 = Color3.fromRGB(28, 24, 36) B.TextColor3 = Color3.fromRGB(150, 140, 160) B.ZIndex = 7 Instance.new("UICorner", B).CornerRadius = UDim.new(0, 5) local st = Instance.new("UIStroke", B) st.Thickness = 1 st.Color = Color3.fromRGB(45, 40, 60) st.ZIndex = 7 B.MouseButton1Click:Connect(function() if not ActiveRunning then return end Config.Aimbot.TargetPart = BoneName DummyIndicator.Text = "Active Target: " .. string.upper(BoneName) for _, child in pairs(DummyFrame:GetChildren()) do if child:IsA("TextButton") then child.BackgroundColor3= Color3.fromRGB(28, 24, 36) child.TextColor3 = Color3.fromRGB(150, 140, 160) end end B.BackgroundColor3 = Color3.fromRGB(160, 50, 255) B.TextColor3 = Color3.fromRGB(255, 255, 255) end) end CreateDummyBtn("H", UDim2.new(0, 60, 0, 45), UDim2.new(0, 115, 0, 5), "Head") CreateDummyBtn("TORSO", UDim2.new(0, 110, 0, 80), UDim2.new(0, 90, 0, 56), "HumanoidRootPart") CreateDummyBtn("L", UDim2.new(0, 40, 0, 65), UDim2.new(0, 45, 0, 56), "LeftHand") CreateDummyBtn("R", UDim2.new(0, 40, 0, 65), UDim2.new(0, 205, 0, 56), "RightHand") CreateDummyBtn("РАНДОМ", UDim2.new(0, 200, 0, 40), UDim2.new(0, 45, 0, 145), "Random") local BoxOptions, BoxDummy, BoxESP, BoxPreview, BoxCosmetics, BoxMisc = AimOptionsG.Parent, DummyG.Parent, VisMainG.Parent, VisPreviewG.Parent, CosG.Parent, MiscThemeG.Parent local function RefreshPages() BoxOptions.Visible = (ActiveTab == "AIM") BoxDummy.Visible = (ActiveTab == "AIM") BoxESP.Visible = (ActiveTab == "VISUALS") BoxPreview.Visible = (ActiveTab == "VISUALS") BoxCosmetics.Visible = (ActiveTab == "COSMETICS") BoxMisc.Visible = (ActiveTab == "MISC") end local function AddTabButton(Text, TabID) local Tab = Instance.new("TextButton", CategoriesContainer) Tab.Size = UDim2.new(0, 60, 0, 30) Tab.BackgroundTransparency = 1 Tab.Text = Text Tab.TextColor3 = (TabID == "AIM") and Color3.fromRGB(160, 50, 255) or Color3.fromRGB(130, 120, 140) Tab.Font = Enum.Font.GothamBold Tab.TextSize = 11 Tab.ZIndex = 6 Tab.MouseButton1Click:Connect(function() if not ActiveRunning then return end ActiveTab = TabID for pName, pFrame in pairs(Pages) do pFrame.Visible = (pName == TabID) end for _, t in pairs(CategoriesContainer:GetChildren()) do if t:IsA("TextButton") then t.TextColor3 = Color3.fromRGB(130, 120, 140) end end Tab.TextColor3 = Color3.fromRGB(160, 50, 255) RefreshPages() end) end AddTabButton("AIMBOT", "AIM") AddTabButton("VISUALS", "VISUALS") AddTabButton("COSMETICS", "COSMETICS") AddTabButton("MISC", "MISC") RefreshPages() local MenuFireflies = {} for i = 1, 35 do local Flie = Instance.new("Frame", BackgroundBlur) Flie.Size = UDim2.new(0, 4, 0, 4) Flie.BackgroundColor3 = Color3.fromRGB(200, 255, 255) Flie.BorderSizePixel = 0 Flie.Position = UDim2.new(math.random(), 0, math.random(), 0) Instance.new("UICorner", Flie).CornerRadius = UDim.new(1, 0) table.insert(MenuFireflies, Flie) end local CapeFolder = Instance.new("Folder") CapeFolder.Name = "TWKS_3DCape_Folder" local function Destroy3DCape() CapeFolder:ClearAllChildren() end local function Update3DCape(Char, Color) if not Config.Cosmetics.Rainbow3DCape or not ActiveRunning then Destroy3DCape() return end local Torso = Char:FindFirstChild("UpperTorso") or Char:FindFirstChild("Torso") if not Torso then return end if CapeFolder.Parent ~= Char then CapeFolder.Parent = Char end local CapePart = CapeFolder:FindFirstChild("MainCape") if not CapePart then CapePart = Instance.new("Part") CapePart.Name = "MainCape" CapePart.Size = Vector3.new(1.8, 3.2, 0.1) CapePart.CanCollide = false CapePart.Massless = true CapePart.Material = Enum.Material.Neon CapePart.Parent = CapeFolder end local Velocity = Torso.AssemblyLinearVelocity.Magnitude local WalkAngle = math.clamp(Velocity * 2.2, 0, 65) local WaveEffect = math.sin(tick() * 7.5) * (1.5 + Velocity * 0.25) CapePart.Color = Color CapePart.CFrame = Torso.CFrame * CFrame.new(0, 0, 0.55) * CFrame.Angles(math.rad(-10 - WalkAngle - WaveEffect), math.rad(0), math.rad(0)) * CFrame.new(0, -1.6, 0) end local LinesCache = {} local function CleanSkeletons() for Player, tbl in pairs(LinesCache) do for _, line in pairs(tbl) do line.Visible = false end end end local function UpdateSkeletons(Color) if not Config.ESP.Enabled or not Config.ESP.Skeleton or not ActiveRunning then CleanSkeletons() return end for _, Player in pairs(Players:GetPlayers()) do if Player ~= LocalPlayer and Player.Character then if Config.ESP.TeamCheck and not CheckTeam(Player) then if LinesCache[Player] then for _, line in pairs(LinesCache[Player]) do line.Visible = false end end continue end if Config.ESP.WallCheck and not (VisibilityCache[Player] or false) then if LinesCache[Player] then for _, line in pairs(LinesCache[Player]) do line.Visible = false end end continue end local Char = Player.Character local H = Char:FindFirstChild("Head") local T = Char:FindFirstChild("UpperTorso") or Char:FindFirstChild("Torso") or Char:FindFirstChild("LowerTorso") if H and T then if not LinesCache[Player] then LinesCache[Player] = {} for i = 1, 2 do table.insert(LinesCache[Player], Drawing.new("Line")) end end local l = LinesCache[Player] local pH, oH = Camera:WorldToViewportPoint(H.Position) local pT, oT = Camera:WorldToViewportPoint(T.Position) if oH and oT then l.Visible = true l.From = Vector2.new(pH.X, pH.Y) l.To = Vector2.new(pT.X, pT.Y) l.Color = Color l.Thickness = 2 l.Transparency = 1 else l.Visible = false end else if LinesCache[Player] then for _, line in pairs(LinesCache[Player]) do line.Visible = false end end end else if LinesCache[Player] then for _, line in pairs(LinesCache[Player]) do line.Visible = false end end end end end local function CreateESPOndGame(Player) if Player == LocalPlayer then return end local function ApplyESP(Char) if not Char then return end local Root = Char:WaitForChild("HumanoidRootPart", 5) local Hum = Char:WaitForChild("Humanoid", 5) if not Root or not Hum then return end local BGui = Char:FindFirstChild("TWKS_BGui") or Instance.new("BillboardGui") BGui.Name = "TWKS_BGui" BGui.Size = UDim2.new(0, 200, 0, 50) BGui.AlwaysOnTop = true BGui.ExtentsOffset = Vector3.new(0, 3, 0) BGui.Adornee = Root BGui.Parent = Char local NameLbl = BGui:FindFirstChild("NLbl") or Instance.new("TextLabel", BGui) NameLbl.Name = "NLbl" NameLbl.Size = UDim2.new(1, 0, 0, 15) NameLbl.BackgroundTransparency = 1 NameLbl.TextColor3 = Color3.fromRGB(255, 255, 255) NameLbl.Font = Enum.Font.GothamBold NameLbl.TextSize = 12 NameLbl.Visible = false Instance.new("UIStroke", NameLbl).Thickness = 1 local HP_Cont = BGui:FindFirstChild("HPCont") or Instance.new("Frame", BGui) HP_Cont.Name = "HPCont" HP_Cont.Size = UDim2.new(0, 60, 0, 4) HP_Cont.Position = UDim2.new(0.5, -30, 0, 18) HP_Cont.BackgroundColor3 = Color3.fromRGB(30, 30, 35) HP_Cont.Visible = false local HP_Fill = Instance.new("Frame", HP_Cont) HP_Fill.Size = UDim2.new(1, 0, 1, 0) HP_Fill.BackgroundColor3 = Color3.fromRGB(0, 255, 100) HP_Fill.BorderSizePixel = 0 local RenderConn RenderConn = RunService.RenderStepped:Connect(function() if not ActiveRunning or not Player.Parent or not Char.Parent or not Hum or Hum.Health <= 0 then BGui.Enabled = false if not Player.Parent then RenderConn:Disconnect() end return end if Config.ESP.TeamCheck and Player.Team == LocalPlayer.Team then BGui.Enabled = false return end if Config.ESP.WallCheck and not (VisibilityCache[Player] or false) then BGui.Enabled = false return end local active = Config.ESP.Enabled BGui.Enabled = active if active then NameLbl.Visible = Config.ESP.Names NameLbl.Text = Player.Name .. " [" .. math.floor((Root.Position - Camera.CFrame.Position).Magnitude) .. "m]" HP_Cont.Visible = Config.ESP.Health local hpPerc = math.clamp(Hum.Health / Hum.MaxHealth, 0, 1) HP_Fill.Size = UDim2.new(hpPerc, 0, 1, 0) HP_Fill.BackgroundColor3 = Color3.fromHSV(hpPerc * 0.35, 1, 1) NameLbl.TextColor3 = Config.ESP.Rainbow and Color3.fromHSV(RainbowHue, 1, 1) or Config.ESP.Color end end) end Player.CharacterAdded:Connect(ApplyESP) if Player.Character then task.spawn(ApplyESP, Player.Character) end end Players.PlayerAdded:Connect(CreateESPOndGame) for _, p in pairs(Players:GetPlayers()) do CreateESPOndGame(p) end local function ToggleMenu(State) MenuState = State if State then BackgroundBlur.Visible = true MainFrame.Visible = true TweenService:Create(MainFrame, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.new(0, 660, 0, 480), Position = UDim2.new(0.5, -330, 0.5, -240)}):Play() TweenService:Create(BackgroundBlur, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {BackgroundTransparency = 0.6}):Play() TweenService:Create(InGameBlur, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Size = 14}):Play() UserInputService.MouseIcon = "rbxasset://textures/Blank.png" else TweenService:Create(MainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Size = UDim2.new(0, 0, 0, 0), Position = UDim2.new(0.5, 0, 0.5, 0)}):Play() TweenService:Create(BackgroundBlur, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {BackgroundTransparency = 1}):Play() TweenService:Create(InGameBlur, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Size = 0}):Play() task.delay(0.3, function() if not MenuState then MainFrame.Visible = false BackgroundBlur.Visible = false end end) UserInputService.MouseIcon = "" end end local function DoUnload() ActiveRunning = false Destroy3DCape() CleanSkeletons() UserInputService.MouseIconEnabled = true UserInputService.MouseIcon = "" InGameBlur.Enabled = false BackgroundBlur.Visible = false Cursor_UI.Visible = false FOV_UI.Visible = false ScreenGui:Destroy() end CloseBtn.MouseButton1Click:Connect(DoUnload) UnloadBox.MouseButton1Click:Connect(DoUnload) task.spawn(function() while task.wait(0.2) do if not ActiveRunning then break end for _, Player in pairs(Players:GetPlayers()) do if Player ~= LocalPlayer and Player.Character then if not Config.ESP.WallCheck then VisibilityCache[Player] = true else local H = Player.Character:FindFirstChild("Head") if H then VisibilityCache[Player] = IsVisibleRay(H, Player.Character) else VisibilityCache[Player] = false end end end end end end) RunService.RenderStepped:Connect(function(Delta) if not ActiveRunning then return end UserInputService.MouseIconEnabled = false RainbowHue = (RainbowHue + Delta * 0.05) % 1 local C1 = Color3.fromHSV(RainbowHue, 0.85, 0.25) local C2 = Color3.fromHSV((RainbowHue + 0.35) % 1, 0.85, 0.15) local StrokeC = Color3.fromHSV(RainbowHue, 1, 1) if MainFrame and MainFrame.Parent then if Config.Menu.Rainbow then MainGradient.Color = ColorSequence.new({ColorSequenceKeypoint.new(0, C1), ColorSequenceKeypoint.new(0.5, C2), ColorSequenceKeypoint.new(1, C1)}) MainStroke.Color = StrokeC else MainStroke.Color = Color3.fromRGB(60, 50, 80) end if ActiveTab == "VISUALS" then local targetColor = Config.ESP.Rainbow and StrokeC or Config.ESP.Color for _, part in pairs(RobloxParts) do if Config.ESP.Highlight then part.BackgroundColor3 = targetColor else part.BackgroundColor3 = Color3.fromRGB(35, 30, 45) end end for _, str in pairs(P_Strokes) do str.Color = targetColor end P_Name.TextColor3 = Config.ESP.Rainbow and StrokeC or Color3.fromRGB(255, 255, 255) P_HPText.TextColor3 = Config.ESP.Rainbow and StrokeC or Color3.fromRGB(0, 255, 100) P_HPFill.BackgroundColor3 = Config.ESP.Rainbow and StrokeC or Color3.fromRGB(0, 255, 100) end end local MousePos = UserInputService:GetMouseLocation() if Config.Aimbot.ShowFOV then FOV_UI.Visible = true FOV_UI.Size = UDim2.new(0, Config.Aimbot.FOV * 2, 0, Config.Aimbot.FOV * 2) if MenuState or Config.Settings.FollowMouse then FOV_UI.Position = UDim2.new(0, MousePos.X, 0, MousePos.Y) else FOV_UI.Position = UDim2.new(0.5, 0, 0.5, 0) end FOV_Stroke.Color = Config.Aimbot.Enabled and StrokeC or Config.Aimbot.FOVColor else FOV_UI.Visible = false end if MenuState or Config.Settings.FollowMouse then Cursor_UI.Position = UDim2.new(0, MousePos.X, 0, MousePos.Y) if MenuState then CursorRot = (CursorRot + Delta * 140) % 360 Cursor_UI.Rotation = CursorRot else CursorRot = 0 Cursor_UI.Rotation = 0 end else Cursor_UI.Position = UDim2.new(0.5, 0, 0.5, 0) CursorRot = 0 Cursor_UI.Rotation = 0 end for _, c in pairs(Cursor_UI:GetChildren()) do if c:IsA("Frame") then c.BackgroundColor3 = Config.Aimbot.Enabled and StrokeC or Color3.fromRGB(0, 220, 255) end end if Config.Aimbot.Enabled and UserInputService:IsMouseButtonPressed(Enum.UserInputType.MouseButton2) then local Target = GetClosestPlayer() if Target then Camera.CFrame = Camera.CFrame:Lerp(CFrame.new(Camera.CFrame.Position, Target.Position), (1 / Config.Aimbot.Smoothness)) end end if BackgroundBlur and BackgroundBlur.Visible then for _, f in pairs(MenuFireflies) do local TargetY = f.Position.Y.Scale - 0.0022 if TargetY < 0 then TargetY = 1 end f.Position = UDim2.new(f.Position.X.Scale + (math.sin(tick() * 1.2 + f.Position.Y.Scale)/850), 0, TargetY, 0) f.BackgroundColor3 = StrokeC end end if LocalPlayer.Character then Update3DCape(LocalPlayer.Character, StrokeC) end UpdateSkeletons(StrokeC) end) task.spawn(function() while true do task.wait(0.1) if ActiveRunning then for _, Player in pairs(Players:GetPlayers()) do if Player ~= LocalPlayer and Player.Character then if Config.ESP.TeamCheck and Player.Team == LocalPlayer.Team then local OldH = Player.Character:FindFirstChild("TWKS_Chams") if OldH then OldH.Parent = nil end continue end local Hum = Player.Character:FindFirstChildOfClass("Humanoid") local Root = Player.Character:FindFirstChild("HumanoidRootPart") if Hum and Hum.Health > 0 and Root then if Config.ESP.WallCheck and not (VisibilityCache[Player] or false) then local OldH = Player.Character:FindFirstChild("TWKS_Chams") if OldH then OldH.Parent = nil end continue end local Highlight = Player.Character:FindFirstChild("TWKS_Chams") or Instance.new("Highlight") Highlight.Name = "TWKS_Chams" Highlight.FillTransparency = 0.4 Highlight.OutlineTransparency = 0 if Config.ESP.Enabled and Config.ESP.Highlight then Highlight.Parent = Player.Character local CurrentColor = Config.ESP.Rainbow and Color3.fromHSV(RainbowHue, 1, 1) or Config.ESP.Color Highlight.FillColor = CurrentColor Highlight.OutlineColor = CurrentColor else Highlight.Parent = nil end end end end end end end) UserInputService.InputBegan:Connect(function(input, gpe) if not ActiveRunning then return end if not gpe and (input.KeyCode == Enum.KeyCode.Insert or input.KeyCode == Enum.KeyCode.RightShift) then ToggleMenu(not MenuState) end end) ToggleMenu(true)
